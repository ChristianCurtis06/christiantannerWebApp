import React from 'react';

const Booking: React.FC = () => {
    return (
        <section id="booking">
            <h2 className="section-header"></h2>
            
            <h3 className="booking-header">Ready to Start Your Project?</h3>
            <p>Let's talk</p>
            <button>Let's Talk</button>
        </section>
    );
};

export default Booking;